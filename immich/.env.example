# Timezone
TZ=America/Chicago

# The Immich version to use. You can pin this to a specific version like "v1.71.0"
IMMICH_VERSION=v2

# Optional machine-learning image tag override. Keep this equal to IMMICH_VERSION for CPU/default ML.
# For hardware acceleration, use an Immich ML backend tag such as v2-cuda or v2-openvino.
IMMICH_ML_VERSION=v2

# Machine-learning hardware acceleration notes:
# - CPU/default: keep IMMICH_VERSION as the normal Immich release tag.
# - CUDA/OpenVINO/ARM NN: set IMMICH_ML_VERSION to the backend image tag described in
#   Immich's ML hardware acceleration docs, and enable the matching service from
#   ./immich/config/hwaccel.ml.yml in compose.yml.
# - Transcoding acceleration is enabled by uncommenting the immich-server extends block
#   in compose.yml and selecting a service from ./immich/config/hwaccel.transcoding.yml.

# The location where your uploaded files are stored
UPLOAD_LOCATION=

# The location where your database files are stored
DB_DATA_LOCATION=

# Redis password for Immich
REDIS_PASSWORD=

# Connection secret for postgres. You should change it to a random password
DB_PASSWORD=

# The values below this line do not need to be changed
DB_USERNAME=postgres
DB_DATABASE_NAME=immich
DB_IMAGE=docker.io/tensorchord/pgvecto-rs:pg14-v0.2.0@sha256:90724186f0a3517cf6914295b5ab410db9ce23190a2d9d0b9dd6463e3fa298f0
